import { Header } from '../Header';
import { Main } from '../Main';

import './MainContainer.scss';

export const MainContainer = () => {
    return (
        <div>
            <Header />
            <Main />
        </div>
    );
}